import React from 'react';
import { Alert } from 'reactstrap';

const Footer = () => (
  <footer className="app-footer">
    <Alert color="primary">
        Page footer
    </Alert>
  </footer>
);

export default Footer;
