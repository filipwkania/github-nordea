import React from 'react';
import { Alert } from 'reactstrap';

const Header = () => (
  <header className="app-header">
    <Alert color="success">
      Hello!
    </Alert>
  </header>
);

export default Header;
